function max(x, y) {
    return x > y ? x : y;  //Se x for maior que Y return x se nao ":" return y
}

const maxExemplo2 = (x, y) => {
    return x > y ? x : y; //Se x for maior que Y return x se nao ":" return y
}

console.log(maxExemplo2(100, 200))